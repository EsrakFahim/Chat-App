import { Box } from '@mui/material';
import React from 'react';

const MessagePart = () => {
      return (
            <>
                  <Box sx={
                        {
                              flexGrow:1,
                              // background:'#fff'
                        }
                  }>
                  </Box>
            </>
      );
};

export default MessagePart;